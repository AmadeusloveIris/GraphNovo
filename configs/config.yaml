defaults:
  - preprocessing: all_graph
  - encoder: light
  - decoder: light
  - train: train
  - infer: sequence_generation_inference
  - _self_

mode: train
dist: false
task: sequence_generation
train_spec_header_path: 
eval_spec_header_path:
train_dataset_dir:
eval_dataset_dir:

wandb:
  entity: genova
  project: optimal_path
  name: hello_world

